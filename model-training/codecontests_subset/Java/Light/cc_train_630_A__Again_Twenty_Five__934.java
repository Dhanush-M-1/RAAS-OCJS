import java.lang.*;
import java.util.Scanner;
import java.util.*;

public class AgainTwentyfive {
    public static void main(String[] args) {
    	Scanner scan = new Scanner (System.in);
    	//long intNum = scan.nextLong();
    	//long outNum = (int)( Math.pow(5 ,intNum));
    	//String number = Long.toString(outNum);
    	long n =  scan.nextLong();
    	
    		if (n >=0 ){
    		System.out.print("25");
    	} else if (n >= 1000){
    		System.out.print("25");
    	}
    	//String[] again = {"25"};
	/*
    	for (int x = number.length()-2 ; x < number.length(); x++){
    	System.out.print(number.charAt(x));
    	if (n >=0 ){
    		System.out.print("25");
    	}
    			}
    			*/
    		}
		}
